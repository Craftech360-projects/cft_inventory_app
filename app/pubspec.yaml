name: app
description: "A new Flutter project."
publish_to: "none" # Remove this if you plan to publish later
version: 0.1.0

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.6.0"

dependencies:
  cupertino_icons: ^1.0.2
  esc_pos_printer: ^4.1.0
  esc_pos_utils: ^1.1.0
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  qr_flutter: ^4.1.0
  sunmi_printer_plus:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true